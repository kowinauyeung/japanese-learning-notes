dist/
dist-e2e-pwa/
node_modules/
yarn.lock
.serena/

# Committed migration artefacts. These are machine-generated records of what was
# loaded into Firestore, and reformatting them would churn the diff for nothing.
migration/output.json
migration/review.json

# Written by `yarn release` from the commit messages, never by hand. Prettier
# reflows its long reference lines, so leaving it in would fail `format:check`
# on every release branch and put a formatting commit in front of every tag.
CHANGELOG.md

# Generated test output.
coverage/
playwright-report/
test-results/
tests/e2e/__screenshots__/

# Generated by `yarn fonts` from font-config.ts: 363 @font-face rules copied
# from Google's stylesheet with the URLs rewritten. Left alone so regenerating
# it produces a diff of what actually changed upstream, rather than one where
# every rule has been reflowed.
src/fonts.css
